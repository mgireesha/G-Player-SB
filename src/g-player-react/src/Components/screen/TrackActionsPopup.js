import React from "react";

export const TrackActionsPopup = ({styles}) => {
    console.log("styles",styles);
    return(
        <div className="track-actions-popup" style={styles}>
            Delete
        </div>
    );
}